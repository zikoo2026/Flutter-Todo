
**وصف موجز**

تطبيق قائمة مهام (Todo) مبني باستخدام Flutter ويعتمد على مكتبة GetX لإدارة الحالة والتنقّل، وHive/SQFLite للتخزين المحلي. يهدف إلى تقديم واجهة بسيطة لإدارة المهام والفئات مع دعم التوطين وتغيير السمات.

---

## ✨ الميزات الرئيسية
- إضافة، تعديل، وحذف المهام والفئات
- تخزين محلي باستخدام Hive و/أو SQLite
- إدارة الحالة والروابط باستخدام GetX
- تبديل السمات (فاتح/داكن)
- دعم تعدد اللغات (عربي/إنجليزي عبر `intl` وملفات الترجمة)

---

## 📦 المتطلبات
- Flutter SDK (متوافق مع `sdk: ^3.10.0` كما في `pubspec.yaml`)
- محرر مثل VS Code أو Android Studio
- جهاز أو محاكي Android / iOS أو بيئة Web/Desktop حسب الحاجة

---

## ⚙️ خطوات الإعداد والتشغيل
1. استنساخ المستودع:

```bash
git clone <رابط المشروع>
cd Flutter-Todo
```

2. تثبيت التبعيات:

```bash
flutter pub get
```

3. (إن كنت تستخدم Hive مع مولّد الشيفرة) توليد الملفات المولدة:

```bash
flutter pub run build_runner build --delete-conflicting-outputs
```

4. تشغيل التطبيق:

```bash
flutter run
```

---

## 🧭 ملاحظات للمطورين
- نماذج البيانات (`Task`, `Category`) قد تستخدم `TypeAdapter` الخاص بـ Hive؛ بعد تعديل أي نموذج تأكد من إعادة تشغيل `build_runner` لتوليد `*.g.dart`.
- ستجد منطق التحكم في `lib/controllers/` (مثل `task_controller.dart`, `category_controller.dart`, `theme_controller.dart`, `locale_controller.dart`).
- تحقق من `pubspec.yaml` للمكتبات الأساسية: `hive`, `hive_flutter`, `sqflite`, `path`, `get`, `intl`.

---

## 📁 هيكل المشروع (موجز)
- `lib/`
  - `main.dart` — نقطة الدخول
  - `controllers/` — GetX controllers
  - `models/` — نماذج البيانات وملفات `*.g.dart`
  - `screens/` — شاشات التطبيق
  - `widgets/` — عناصر واجهة قابلة لإعادة الاستخدام
  - `helper/` — توجيهات، سمات، ترجمات، مسارات
